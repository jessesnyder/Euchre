

class Dealer():

    def __init__(self, deck):
        self.deck = deck

    def deal(players):
        self.deck.shuffledcards()
        for p in players:
            hand = self.generateHand()
            p.setHand(hand)
